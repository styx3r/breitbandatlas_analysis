# General

```
usage: analyzer.py [-h] breitbandatlas_csv_file ISP_Name

positional arguments:
  breitbandatlas_csv_file
                        Path to the CSV file of the Breitbandatlast
  ISP_Name              Name of the ISP which should be filtered

optional arguments:
  -h, --help            show this help message and exit
```